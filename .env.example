# Optional future integration. The current UI ALWAYS uses the local demo adapter.
# Public browser-safe project values only. Never add service-role or video secrets.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=


# Sree@854775=superBase password